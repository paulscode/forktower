// Package app wires the daemon together: configuration, storage, the event
// bus, the engines, and the HTTP server, with a single lifecycle that starts
// and stops them in a defined order.
package app
