// Package api serves the HTTP interface and the embedded web dashboard.
//
// User-facing wording is decided here rather than in the browser, so that every
// surface says the same thing and the phrasing can be reviewed and tested.
package api
