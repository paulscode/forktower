// Package alert turns internal events into notifications the user actually
// receives, across one or more delivery channels, with escalation for anything
// urgent and a self-test so that a silent alarm is detected rather than
// assumed to be silence.
package alert
